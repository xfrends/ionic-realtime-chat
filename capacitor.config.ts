import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.realtime.chat',
  appName: 'ionic-realtime-chat',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
